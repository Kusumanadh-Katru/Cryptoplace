import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  base: "/Cryptoplace/", // Make sure the base URL matches the GitHub Pages path
});
